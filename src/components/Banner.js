function Banner() {
    return (
        <div>
            <header>Orbit Report</header>
            <p>Click the 'Orbit Type' buttons to see the satellites in that orbit type.</p>
        </div>
    );
}

export default Banner;
